networks:
  webnet:
    driver: overlay
    attachable: true
    services:
  web:
    image: "matisskalnins/cm-backend:latest"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    ports:
      - "50051:50051"
    networks:
      - webnet